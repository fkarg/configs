---

# git username of the repos to be pulled
git_username: fkarg
configs_repo: configs
dotconfigdir: dotconfig
todotxtrepo: gtd
finances_repo: finances

# username on this machine
username: pars

# cowfile on this machine
cowfile: default

# terminal for machine
terminal: kitty

# if the nixos-config should be linked on this system
link_nixos: false

# absolute path to background image to use for graphical systems. Default is one-screen.
background_image: /home/{{ username }}/{{ configs_repo }}/background_picture/IMAG7297.JPG

# name of the audio sink
pactl_sink_name: 0

# git configs
git_email: f.karg10@gmail.com
git_name: Felix Karg
git_editor: nvim

# ensure that nix is installed regardless of of the OS being nixOS
ensure_nix: true

# if nix is available, install a number of packages for the user
install_nix_programs: true


repos_list:
  - "/home/$USER/configs"
  - "/home/$USER/finances"
  - "/home/$USER/gtd"
  - "/home/$USER/text_zeug"
  - "/home/$USER/Coding/lebenslauf"
  - "/home/$USER/Coding/invoices"
  - "/home/$USER/Coding/kit-stuff"
  - "/home/$USER/Coding/fkarg.github.io"
  - "/home/$USER/Coding/beratervertrag"
  - "/home/$USER/Coding/things-to-talk-about"

repos_additional: []
