---
description: Switch between saved Codex accounts
argument-hint: status|list|save <profile>|use <profile>|login <profile> [--device-auth]
---

Run `codex-account $ARGUMENTS` and show the important output.

If the command switches or logs in to a profile, remind me to start a new Codex
CLI or IDE session before relying on the newly selected account.
