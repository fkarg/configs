---
# Personal Mac. No bootstrap/base — managed locally via ansible-pull.
ansible_connection: local
ansible_user: felix

host_roles:
  - terminal_dotfiles
  - personal
  - os_macos
  - graphical_dotfiles
  - coding_agents
  - agent_sessions_sync

# identity
username: felix
cowfile: llama

# shell integrations
direnv: true
zoxide: true
claudex_enabled: true

# Monitor KVM (Philips 49B2U5900CH): Ctrl+Shift+F12 sends a DDC input-switch
# from this host to the *other* machine on the shared monitor. Ctrl+Shift+F11
# does the same, then sleeps caeli so its USB-C signal drops and the monitor
# can't auto-hop back while jolly is still booting.
# caeli is on USB-C, so kvm_switch_target_input is jolly's input code on the
# Philips.
# Discover with:
#   m1ddc display list                              # copy the Philips UUID
#   m1ddc display <UUID> get input                  # while sitting on USB-C
#   # then switch to jolly via the monitor's OSD and rerun:
#   m1ddc display <UUID> get input                  # → that's the value below
# kvm_display_id pins the command to the Philips even if display ordering or
# the built-in's connection state changes. 15 == 0x0F is jolly's verified
# DisplayPort-1 input.
kvm_switch_enabled: true
kvm_display_id: "0A6C28AE-EE62-41C3-8565-25FEE528B91B"
# kvm_self_input is *this* host's input on the monitor (caeli = USB-C). When
# set, the keybind toggles between self and target instead of always firing
# at target; leave null for one-shot behaviour. Discover with
# `m1ddc display <id> get input` while connected normally.
kvm_self_input: null
kvm_switch_target_input: 15

# Ctrl-as-Cmd: map Ctrl+<key> -> Cmd+<key> in GUI apps so Linux muscle memory
# (copy/paste/close-tab/...) works here, while Cmd keeps working. Terminals are
# excluded (Ctrl stays SIGINT/readline). Still needs a one-time enable in
# Karabiner → Complex Modifications → Add rule. Tune via ctrl_as_cmd_keys /
# ctrl_as_cmd_terminals if needed.
ctrl_as_cmd_enabled: true
# Neo2 override: the remap matches hardware key_codes (US-QWERTY positions), but
# under the Neo2 input source the character 'a' lives on the physical D key
# (key_code d). Without 'd', Ctrl+<our 'a'> leaks through as a raw Ctrl+a, which
# macOS's Cocoa text system treats as ^A = "beginning of line" (the bug). Adding
# 'd' makes it emit Cmd+key_code d → Cmd+a under Neo2 → select-all. Copy/paste/
# close/new-tab already work because their Neo2 keys land on r/w/t/l, which the
# default list happens to include. The rest of the default letters are harmless
# no-ops on Neo2 (wrong physical key). Extend this list if cut/undo/find/... are
# also wanted (they sit on q/b/o/... — verify each key_code in Karabiner-EventViewer).
ctrl_as_cmd_keys: [c, v, x, z, a, w, t, f, n, s, r, l, d, o, return_or_enter]

# brew update/upgrade happens manually here often enough that baking a ~2 min
# update/upgrade cycle into every config run is just friction. Skip it by
# default; force a one-off with `-e brew_upgrade_enabled=true`.
brew_upgrade_enabled: false

# INVARIANT — Top-level only. (See spec for refresh procedure.)
# Both `brew_packages_common` and `brew_casks_common` below must list ONLY
# top-level (manually installed) packages, never transitive dependencies.
# Sources of truth: `brew leaves` and `brew list --cask` on the live host.

brew_packages_common:
  # shell, prompt & file utilities
  - bat
  - btop
  - coreutils
  - cowsay
  - direnv
  - eza
  - fish
  - fortune
  - fzf
  - htop
  - lf
  - ncdu
  - fastfetch
  - ripgrep-all
  # GNU rsync. macOS ships openrsync, which honours --update but doesn't
  # document it; agent-sync's conflict rule depends on that flag, so pin the
  # same implementation the Linux hosts run rather than trust undocumented
  # behaviour to survive an OS update.
  - rsync
  - watch
  - wget
  - yq
  - zellij
  - zoxide
  # editor, language servers & linters
  - actionlint
  - languagetool
  - neovim
  - tinymist
  - yamllint
  # git & dev workflow
  - gh
  - git
  - git-filter-repo
  - glow
  - lazygit
  - tokei
  # programming languages & runtimes
  - ghc
  - node@24
  - oven-sh/bun/bun
  - pnpm
  - python-setuptools
  - rust
  - uv
  # containers & kubernetes
  - docker
  - docker-buildx
  - k3d
  - k9s
  - kubecfg
  - kubernetes-cli
  - kustomize
  # networking & remote access
  - iperf
  - ldns
  - mosh
  - nmap
  - socat
  - wireguard-tools
  - xpipe
  # storage & databases
  - minio-mc
  - postgresql@17
  # documents & typesetting
  - ghostscript
  - graphviz
  - psutils
  - qpdf
  - svgbob
  - typst
  # media
  - ffmpeg
  - yt-dlp
  # finance / accounting
  - beancount
  - beancount-language-server
  - beanquery
  - fava
  # infrastructure / config management
  - ansible
  - ansible-language-server
  - ansible-lint
  # AI / coding agents
  - ccusage
  - gemini-cli
  - opencode
  - pi-coding-agent
  # macOS-specific helpers
  - batt
  - m1ddc
  - pngpaste
  - sleepwatcher
brew_packages_extra: []

brew_casks_common:
  # window management & system tweaks
  - amethyst
  - caffeine
  - karabiner-elements
  # openlogi: local-first Logi Options+ replacement (HID++ devices).
  - openlogi
  - raycast
  - rectangle
  - scroll-reverser
  - stats
  # terminals & editors
  - kitty
  - visual-studio-code
  # browsers
  - firefox
  - google-chrome
  - tor-browser
  # communication & messaging
  - betterdiscord-installer
  - discord
  - element
  - microsoft-teams
  - signal
  - telegram
  - telegram-desktop
  - whatsapp
  # proton suite
  - proton-drive
  - proton-mail
  - proton-pass
  - protonvpn
  # microsoft office
  - microsoft-excel
  - microsoft-outlook
  - microsoft-powerpoint
  - microsoft-word
  # productivity & documents
  - libreoffice
  - mark-text
  - obsidian
  - zotero
  # AI / coding agents
  - claude-code@latest
  - codex
  - comfyui
  - copilot-cli
  - ollama-app
  # containers & virtualization
  - docker-desktop
  - utm
  # media
  - vlc
  - youtube-to-mp3
  # misc / experimental
  - pear-desktop
brew_casks_extra: []
